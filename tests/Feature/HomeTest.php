<?php

test('My name can be entered', function () {
    $this->assertTrue(false);
});

test('My date of birth can entered using a date picker', function () {
    $this->assertTrue(false);
});

test('my age in years days and hours is displayed', function () {
    $this->assertTrue(false);
});

test('Name and age of previous submissions can be accessed', function () {
    $this->assertTrue(false);
});
